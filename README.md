# Weather Checker API

A simple Python application that retrieves and displays weather information using an external weather API.

This project demonstrates how to integrate third-party APIs, handle HTTP requests, parse JSON responses, and present data in a user-friendly way.

---

## ☁️ Overview

This tool connects to a weather API to fetch real-time weather conditions for any city specified by the user.

Core features:

✔ Fetch current weather using API  
✔ Display key metrics (temperature, conditions, humidity, etc.)  
✔ Handles invalid city input gracefully  
✔ Simple and clear output format

---

## 🌤️ Why This Is Useful

Working with APIs is a fundamental engineering skill. This project shows you can:

- Send HTTP requests
- Parse JSON data
- Interpret API responses
- Deal with errors and user input

Those are essential for backend, data, and integration work.

---

## 🚀 Get Started

### 📋 Requirements

- Python 3.7+
- `requests` library

Install dependencies:

```bash
pip install requests
